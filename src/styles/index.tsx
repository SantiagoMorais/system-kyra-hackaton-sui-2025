import { tv } from "tailwind-variants";

export const scrollBar = tv({
  base: "scrollbar-thumb-rounded-full scrollbar-track-rounded-full scrollbar scrollbar-thumb-orange-600 scrollbar-track-grey/30 pr-2",
});
